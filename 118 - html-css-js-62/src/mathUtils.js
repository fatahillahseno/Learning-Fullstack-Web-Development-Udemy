// export agar bisa memberikan izin import pada file lain untuk menggunakan komponen yang ada di dalam file ini
export function add(x, y) {
  return x + y;
}
export function subtract(x, y) {
  return x - y;
}
export const pi = 3.14159;
